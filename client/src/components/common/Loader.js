import React from 'react';

const Loader = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center'
      }}
    >
      <img src="/img/loader.gif" alt="" />
    </div>
  );
};

export default Loader;
